<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View"/>
        <variable
            name="header"
            type="String" />
        <variable
            name="viewModel"
            type="com.example.fastfooddiet.viewmodels.CategoryListViewModel" />
    </data>
<LinearLayout
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/list_cat_frag_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#81FF2121"
        app:title=" "
        tools:navigationIcon="?homeAsUpIndicator">

        <androidx.appcompat.widget.SearchView
            android:id="@+id/list_cat_frag_searchView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:iconifiedByDefault="false"
            app:searchIcon="@null" />

    </androidx.appcompat.widget.Toolbar>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/list_cat_frag_header"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginBottom="24dp"
            android:layout_weight="1"
            android:fontFamily="@font/roboto_thin"
            android:text="@{header}"
            android:textAlignment="textStart"
            android:textSize="24sp" />

        <ImageButton
            android:id="@+id/list_cat_frag_next_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="Next"
            android:visibility="@{viewModel.isMultipleSelect ? View.VISIBLE : View.INVISIBLE}"
            app:srcCompat="@android:drawable/ic_media_play" />

    </LinearLayout>


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list_cat_frag_recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>
</layout>